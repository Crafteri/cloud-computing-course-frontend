# Front-end that is for a sentiment analysis model

- Requires Nodejs and NPM

Place the base path for the backend in `.env`.

Once you are in the folder you can run for example:
```bash
$ npm install
$ npm run dev
```
